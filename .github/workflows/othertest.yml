name: other test
on:
  push:
    paths-ignore:
      - '**/workflows/mailtest.yml'

  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: true
      matrix:
        os: [windows-latest]
        python: ['3.10']
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
      with:
        persist-credentials: false
        repository: 'sebbASF/cpython'
        ref: 'gh-93376'
    - name: Check
      run: |
        python3 -V
        ls
        ./configure
        ls
        make
        make test TESTOPTS: "-v test_mailbox"
        
